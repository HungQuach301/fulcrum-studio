name: WP-002 Foundation Acceptance
permissions:
  contents: read
  pull-requests: read
  actions: read
concurrency:
  group: ${{ github.event_name == 'workflow_dispatch' && 'fs24-d-controller' || format('fs24-d-check-{0}-{1}', github.ref, github.event_name)
    }}
  cancel-in-progress: false
env:
  FS_HEAD: ${{ inputs.request && fromJSON(inputs.request).code || github.event.workflow_run.head_sha || github.event.pull_request.head.sha ||
    github.sha }}
  FS_BRANCH: ${{ github.head_ref || github.ref_name }}
  FS_TITLE: ${{ github.event.pull_request.title || '' }}
  FS_EVENT: ${{ github.event_name }}
  GH_TOKEN: ${{ github.token }}
  FS_GRANT: FS24-D
  FS24_CONTROL: ${{ inputs.request || '' }}
jobs:
  foundation:
    name: foundation
    runs-on: ubuntu-24.04
    env:
      FS_JOB: foundation
      FS_PRODUCER: foundation
    steps:
    - name: Initialize evidence directory
      id: init
      shell: bash
      run: |
        set -euo pipefail
        task_root="$(mktemp -d "$RUNNER_TEMP/fs24-$FS_JOB.XXXXXX")"
        mkdir "$task_root/evidence"
        printf 'TASK_ROOT=%s
        FS_EVIDENCE=%s/evidence
        ' "$task_root" "$task_root" >> "$GITHUB_ENV"
    - name: Checkout exact candidate
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        ref: ${{ env.FS_HEAD }}
        fetch-depth: 0
        persist-credentials: false
    - name: Read-only source admission
      id: preflight
      shell: bash
      run: |
        set -euo pipefail
        python3 "$GITHUB_WORKSPACE/scripts/wp002-preflight.py" runtime
    - name: Checksum-pinned runtime
      id: runtime
      shell: bash
      run: |
        set -euo pipefail
        curl --fail --silent --show-error --location https://nodejs.org/dist/v20.20.2/node-v20.20.2-linux-x64.tar.xz -o "$TASK_ROOT/node.tar.xz"
        printf '%s  %s\n' df770b2a6f130ed8627c9782c988fda9669fa23898329a61a871e32f965e007d "$TASK_ROOT/node.tar.xz" | sha256sum -c -
        tar -xJf "$TASK_ROOT/node.tar.xz" -C "$TASK_ROOT"
        node_path="$TASK_ROOT/node-v20.20.2-linux-x64/bin/node"
        npm_cli="$TASK_ROOT/node-v20.20.2-linux-x64/lib/node_modules/npm/bin/npm-cli.js"
        test "$($node_path --version)" = v20.20.2
        test "$($node_path "$npm_cli" --version)" = 10.8.2
        printf 'FS_NODE=%s\nFS_NPM=%s\n' "$node_path" "$npm_cli" >> "$GITHUB_ENV"
        python3 - <<'PY'
        import os,json,pathlib
        pathlib.Path(os.environ['FS_EVIDENCE'],'runtime.json').write_text(json.dumps({'node':'20.20.2','npm':'10.8.2','archiveSha256':'df770b2a6f130ed8627c9782c988fda9669fa23898329a61a871e32f965e007d','archiveChecksum':'pass','node20EolException':'FS24-D-only','actionNode24Patch':'unproven','runnerOS':os.environ.get('RUNNER_OS'),'runnerArch':os.environ.get('RUNNER_ARCH'),'imageVersion':os.environ.get('ImageVersion'),'billedCostUsd':None,'B1B2':'unverified'})+'\n')
        PY
    - name: Set up action runtime tooling
      id: setup
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
      with:
        node-version: '20'
    - name: Install exact lockfile without lifecycle scripts
      id: install
      shell: bash
      run: |
        set -euo pipefail
        mkdir "$TASK_ROOT/mirror"
        git archive "$FS_HEAD" | tar -x -C "$TASK_ROOT/mirror"
        printf 'FS_TSX=%s/mirror/node_modules/tsx/dist/cli.mjs\n' "$TASK_ROOT" >> "$GITHUB_ENV"
        export npm_config_cache="$TASK_ROOT/npm-cache"
        set +e
        "$FS_NODE" "$FS_NPM" ci --prefix "$TASK_ROOT/mirror" --ignore-scripts --no-audit --no-fund --include=dev --include=optional > "$FS_EVIDENCE/npm-ci.stdout.txt" 2> "$FS_EVIDENCE/npm-ci.stderr.txt"
        code=$?
        set -e
        printf '%s\n' "$code" > "$FS_EVIDENCE/npm-ci.exit.txt"
        exit "$code"
    - name: Run foundation model and registry checks
      id: check
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PY'
        import os,json,pathlib,subprocess,hashlib
        e=pathlib.Path(os.environ['FS_EVIDENCE']);mirror=pathlib.Path(os.environ['TASK_ROOT'],'mirror');node=os.environ['FS_NODE'];tsx=os.environ['FS_TSX'];root=os.environ['GITHUB_WORKSPACE'];head=os.environ['FS_HEAD'];base=os.environ['FS_BASE'];job=os.environ['FS_JOB']
        commands=[('repo-store-tests',[node,tsx,'engine/io/repo-store.test.ts']),('registry-tests',[node,tsx,'engine/providers/registry.test.ts']),('integration-tests',[node,tsx,'engine/io/wp002-integration.test.ts']),('github-git-tests',[node,tsx,'engine/io/github-git.test.ts'])]
        receipts=[];failed=False
        for name,args in commands:
         with (e/(name+'.stdout.txt')).open('wb') as out,(e/(name+'.stderr.txt')).open('wb') as err:
          r=subprocess.run(args,cwd=mirror,stdout=out,stderr=err,env=os.environ.copy())
         row={'name':name,'args':args,'cwd':str(mirror),'status':r.returncode}
         for stream in ['stdout','stderr']:
          data=(e/(name+'.'+stream+'.txt')).read_bytes();row[stream+'Bytes']=len(data);row[stream+'Sha256']=hashlib.sha256(data).hexdigest()
         receipts.append(row);failed|=r.returncode!=0

        manifest=subprocess.run(['python3',str(pathlib.Path(root,'scripts/wp002-preflight.py')),'tests','--suite','foundation'])
        failed|=manifest.returncode!=0
        (e/'commands.json').write_text(json.dumps(receipts,indent=2)+'\n')
        raise SystemExit(1 if failed else 0)
        PY
    - name: Execute diagnose-candidate
      id: diagnose_candidate
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PYCOMMAND'
        mode='diagnose-candidate';target='engine/io/wp002-integration.ts'
        import os,pathlib,subprocess,sys,threading,json,hashlib
        p=pathlib.Path(os.environ['FS_EVIDENCE']);args=[os.environ['FS_NODE'],os.environ['FS_TSX'],target,mode]
        child=subprocess.Popen(args,cwd=pathlib.Path(os.environ['TASK_ROOT'],'mirror'),stdout=subprocess.PIPE,stderr=subprocess.PIPE)
        def drain(source,name,dest):
         with (p/(mode+'.'+name+'.txt')).open('wb') as f:
          for row in iter(lambda:source.read(65536),b''):
           f.write(row);f.flush()
           if os.environ.get('FS_EVIDENCE_MODE')!='bundle-v1':dest.write(row);dest.flush()
        workers=[threading.Thread(target=drain,args=(child.stdout,'stdout',sys.stdout.buffer)),threading.Thread(target=drain,args=(child.stderr,'stderr',sys.stderr.buffer))]
        for w in workers:w.start()
        status=child.wait()
        for w in workers:w.join()
        (p/(mode+'.exit.txt')).write_text(str(status)+'\n')
        receipt={'mode':mode,'args':args,'status':status}
        for stream in ['stdout','stderr']:
         b=(p/(mode+'.'+stream+'.txt')).read_bytes();receipt[stream]={'bytes':len(b),'sha256':hashlib.sha256(b).hexdigest()}
        (p/(mode+'.command.json')).write_text(json.dumps(receipt)+'\n')
        raise SystemExit(status)
        PYCOMMAND
    - name: Execute prepare
      id: prepare
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PYCOMMAND'
        mode='prepare';target='engine/io/wp002-integration.ts'
        import os,pathlib,subprocess,sys,threading,json,hashlib
        p=pathlib.Path(os.environ['FS_EVIDENCE']);args=[os.environ['FS_NODE'],os.environ['FS_TSX'],target,mode]
        child=subprocess.Popen(args,cwd=pathlib.Path(os.environ['TASK_ROOT'],'mirror'),stdout=subprocess.PIPE,stderr=subprocess.PIPE)
        def drain(source,name,dest):
         with (p/(mode+'.'+name+'.txt')).open('wb') as f:
          for row in iter(lambda:source.read(65536),b''):
           f.write(row);f.flush()
           if os.environ.get('FS_EVIDENCE_MODE')!='bundle-v1':dest.write(row);dest.flush()
        workers=[threading.Thread(target=drain,args=(child.stdout,'stdout',sys.stdout.buffer)),threading.Thread(target=drain,args=(child.stderr,'stderr',sys.stderr.buffer))]
        for w in workers:w.start()
        status=child.wait()
        for w in workers:w.join()
        (p/(mode+'.exit.txt')).write_text(str(status)+'\n')
        receipt={'mode':mode,'args':args,'status':status}
        for stream in ['stdout','stderr']:
         b=(p/(mode+'.'+stream+'.txt')).read_bytes();receipt[stream]={'bytes':len(b),'sha256':hashlib.sha256(b).hexdigest()}
        (p/(mode+'.command.json')).write_text(json.dumps(receipt)+'\n')
        raise SystemExit(status)
        PYCOMMAND
      if: ${{ env.FS_CLOSURE != 'true' && env.FS_EVIDENCE_MODE != 'bundle-v1' }}
    - name: Upload new bounded fixture
      id: upload
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: ${{ steps.prepare.outputs.artifact_name }}
        path: ${{ env.TASK_ROOT }}/handoff/*
        retention-days: 7
        if-no-files-found: error
        compression-level: 0
      if: ${{ env.FS_CLOSURE != 'true' && env.FS_EVIDENCE_MODE != 'bundle-v1' }}
    - name: Execute receive
      id: receive
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PYCOMMAND'
        mode='receive';target='engine/io/wp002-integration.ts'
        import os,pathlib,subprocess,sys,threading,json,hashlib
        p=pathlib.Path(os.environ['FS_EVIDENCE']);args=[os.environ['FS_NODE'],os.environ['FS_TSX'],target,mode]
        child=subprocess.Popen(args,cwd=pathlib.Path(os.environ['TASK_ROOT'],'mirror'),stdout=subprocess.PIPE,stderr=subprocess.PIPE)
        def drain(source,name,dest):
         with (p/(mode+'.'+name+'.txt')).open('wb') as f:
          for row in iter(lambda:source.read(65536),b''):
           f.write(row);f.flush()
           if os.environ.get('FS_EVIDENCE_MODE')!='bundle-v1':dest.write(row);dest.flush()
        workers=[threading.Thread(target=drain,args=(child.stdout,'stdout',sys.stdout.buffer)),threading.Thread(target=drain,args=(child.stderr,'stderr',sys.stderr.buffer))]
        for w in workers:w.start()
        status=child.wait()
        for w in workers:w.join()
        (p/(mode+'.exit.txt')).write_text(str(status)+'\n')
        receipt={'mode':mode,'args':args,'status':status}
        for stream in ['stdout','stderr']:
         b=(p/(mode+'.'+stream+'.txt')).read_bytes();receipt[stream]={'bytes':len(b),'sha256':hashlib.sha256(b).hexdigest()}
        (p/(mode+'.command.json')).write_text(json.dumps(receipt)+'\n')
        raise SystemExit(status)
        PYCOMMAND
      env:
        FS_ARTIFACT_ID: ${{ steps.upload.outputs.artifact-id }}
      if: ${{ env.FS_CLOSURE != 'true' && env.FS_EVIDENCE_MODE != 'bundle-v1' }}
    - name: Verify source mirror and live main unchanged
      id: preservation
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        python3 "$GITHUB_WORKSPACE/scripts/wp002-preflight.py" preserve
    - name: Remove temporary execution data
      id: cleanup
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PY'
        import os,pathlib,shutil,json
        p=pathlib.Path(os.environ['TASK_ROOT']);assert p.parent==pathlib.Path(os.environ['RUNNER_TEMP']) and p.name.startswith('fs24-')
        for x in p.iterdir():
         if x.name=='evidence':continue
         if x.is_dir():shutil.rmtree(x)
         else:x.unlink()
        (p/'evidence/cleanup.json').write_text(json.dumps({'executionDataRemoved':True,'reportDataRemainingUntilFrame':True})+'\n')
        PY
    - name: Emit complete evidence bytes
      id: evidence
      if: ${{ always() }}
      shell: bash
      env:
        FS_STEPS: ${{ toJSON(steps) }}
      run: |
        set -euo pipefail
        python3 - <<'PY'
        import os,pathlib,json,hashlib,base64,re
        p=pathlib.Path(os.environ['FS_EVIDENCE']);steps=json.loads(os.environ['FS_STEPS']);(p/'steps-at-emission.json').write_text(json.dumps(steps,indent=2)+'\n')
        # FS24C_REQUIRED_BEGIN
        required=['preflight', 'runtime', 'setup', 'install', 'check', 'diagnose_candidate', 'prepare', 'upload', 'receive', 'preservation', 'cleanup']
        if os.environ.get('FS_CLOSURE')=='true' or os.environ.get('FS_EVIDENCE_MODE')=='bundle-v1':required=[x for x in required if x not in ['prepare','upload','receive']]
        ok=all(steps.get(s,{}).get('outcome')=='success' for s in required)
        # FS24C_REQUIRED_END
        errors=[]
        for f in sorted(p.glob('*.stderr.txt')):
         for line in f.read_text(errors='replace').splitlines():
          if line.strip():errors.append(line[:400])
        for f in sorted(p.glob('*.stdout.txt')):
         for line in f.read_text(errors='replace').splitlines():
          if re.search(r'error TS\d+|AssertionError|Error:',line):errors.append(line[:400])
        def redact(x):
         if re.search(r'\b(?:sk-[A-Za-z0-9_-]{16,}|gh[pousr]_[A-Za-z0-9]{20,}|AKIA[A-Z0-9]{16})\b',x) or ('PRIVATE'+' KEY') in x:return '[redacted]'
         if any(not re.fullmatch('[a-fA-F0-9]+',v) for v in re.findall(r'[A-Za-z0-9+/]{80,}={0,2}',x)):return '[redacted]'
         return x
        summary={'head':os.environ['FS_HEAD'],'base':os.environ.get('FS_BASE',''),'job':os.environ['FS_JOB'],'result':'success' if ok else 'failure','errors':[redact(x) for x in errors[:20]],'finalJobStatus':'requires-API-readback'}
        (p/'job-summary.json').write_text(json.dumps(summary,ensure_ascii=False)+'\n')
        with open(os.environ['GITHUB_OUTPUT'],'a') as out:out.write('summary='+json.dumps(summary,ensure_ascii=True)+'\n')
        identity=['FS24-D',os.environ['GITHUB_RUN_ID'],os.environ['GITHUB_RUN_ATTEMPT'],os.environ['FS_HEAD'],os.environ['FS_JOB'],os.environ['FS_EVENT'],os.environ.get('FS_BASE','')]
        if os.environ.get('FS_EVIDENCE_MODE')=='bundle-v1':
         import subprocess
         target=pathlib.Path(os.environ['TASK_ROOT'],'evidence-bundle.zip')
         subprocess.run(['python3',str(pathlib.Path(os.environ['GITHUB_WORKSPACE'],'engine/io/evidence-transfer.py')),'pack','--directory',str(p),'--file',str(target)],check=True)
         raise SystemExit(0 if ok else 1)
        print('FS24_BEGIN\t'+'\t'.join(identity))
        files=sorted(p.iterdir())
        for f in files:
         assert f.is_file() and re.fullmatch('[A-Za-z0-9_.-]+',f.name)
         data=f.read_bytes();name=base64.b64encode(f.name.encode()).decode();print('FS24_FILE\t'+name+'\t'+str(len(data))+'\t'+hashlib.sha256(data).hexdigest());value=base64.b64encode(data).decode()
         for i in range(0,len(value),2048):print('FS24_DATA\t'+str(i//2048+1)+'\t'+value[i:i+2048])
         print('FS24_END\t'+name)
        print('FS24_COMPLETE\t'+str(len(files)))
        raise SystemExit(0 if ok else 1)
        PY
    - name: Preserve original evidence bundle
      id: evidence_bundle
      if: ${{ always() && env.FS_EVIDENCE_MODE == 'bundle-v1' }}
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: fs24e-${{ github.run_id }}-${{ github.run_attempt }}-${{ env.FS_JOB }}
        path: ${{ env.TASK_ROOT }}/evidence-bundle.zip
        retention-days: 7
        compression-level: 0
        if-no-files-found: error
    - name: Remove final report data
      id: final_cleanup
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        case "$TASK_ROOT" in "$RUNNER_TEMP"/fs24-*) rm -rf -- "$TASK_ROOT" ;; *) exit 1 ;; esac
        test ! -e "$TASK_ROOT"
        printf 'FS24_FINAL_REPORT_CLEANUP=pass;job=%s\n' "$FS_JOB"
    if: ${{ !github.event.deleted && (github.event_name == 'push' || (github.event_name == 'pull_request' && github.head_ref == 'wp/002' && github.event.pull_request.head.repo.full_name
      == github.repository)) }}
  admit:
    name: admit
    runs-on: ubuntu-24.04
    env:
      FS_JOB: admit
    steps:
    - name: Initialize evidence directory
      id: init
      shell: bash
      run: |
        set -euo pipefail
        task_root="$(mktemp -d "$RUNNER_TEMP/fs24-$FS_JOB.XXXXXX")"
        mkdir "$task_root/evidence"
        printf 'TASK_ROOT=%s
        FS_EVIDENCE=%s/evidence
        ' "$task_root" "$task_root" >> "$GITHUB_ENV"
    - name: Checkout exact candidate
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        ref: ${{ env.FS_HEAD }}
        fetch-depth: 0
        persist-credentials: false
    - name: Read-only source admission
      id: preflight
      shell: bash
      run: |
        set -euo pipefail
        python3 "$GITHUB_WORKSPACE/scripts/wp002-preflight.py" runtime
    - name: Checksum-pinned runtime
      id: runtime
      shell: bash
      run: |
        set -euo pipefail
        curl --fail --silent --show-error --location https://nodejs.org/dist/v20.20.2/node-v20.20.2-linux-x64.tar.xz -o "$TASK_ROOT/node.tar.xz"
        printf '%s  %s\n' df770b2a6f130ed8627c9782c988fda9669fa23898329a61a871e32f965e007d "$TASK_ROOT/node.tar.xz" | sha256sum -c -
        tar -xJf "$TASK_ROOT/node.tar.xz" -C "$TASK_ROOT"
        node_path="$TASK_ROOT/node-v20.20.2-linux-x64/bin/node"
        npm_cli="$TASK_ROOT/node-v20.20.2-linux-x64/lib/node_modules/npm/bin/npm-cli.js"
        test "$($node_path --version)" = v20.20.2
        test "$($node_path "$npm_cli" --version)" = 10.8.2
        printf 'FS_NODE=%s\nFS_NPM=%s\n' "$node_path" "$npm_cli" >> "$GITHUB_ENV"
        python3 - <<'PY'
        import os,json,pathlib
        pathlib.Path(os.environ['FS_EVIDENCE'],'runtime.json').write_text(json.dumps({'node':'20.20.2','npm':'10.8.2','archiveSha256':'df770b2a6f130ed8627c9782c988fda9669fa23898329a61a871e32f965e007d','archiveChecksum':'pass','node20EolException':'FS24-D-only','actionNode24Patch':'unproven','runnerOS':os.environ.get('RUNNER_OS'),'runnerArch':os.environ.get('RUNNER_ARCH'),'imageVersion':os.environ.get('ImageVersion'),'billedCostUsd':None,'B1B2':'unverified'})+'\n')
        PY
    - name: Set up action runtime tooling
      id: setup
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
      with:
        node-version: '20'
    - name: Install exact lockfile without lifecycle scripts
      id: install
      shell: bash
      run: |
        set -euo pipefail
        mkdir "$TASK_ROOT/mirror"
        git archive "$FS_HEAD" | tar -x -C "$TASK_ROOT/mirror"
        printf 'FS_TSX=%s/mirror/node_modules/tsx/dist/cli.mjs\n' "$TASK_ROOT" >> "$GITHUB_ENV"
        export npm_config_cache="$TASK_ROOT/npm-cache"
        set +e
        "$FS_NODE" "$FS_NPM" ci --prefix "$TASK_ROOT/mirror" --ignore-scripts --no-audit --no-fund --include=dev --include=optional > "$FS_EVIDENCE/npm-ci.stdout.txt" 2> "$FS_EVIDENCE/npm-ci.stderr.txt"
        code=$?
        set -e
        printf '%s\n' "$code" > "$FS_EVIDENCE/npm-ci.exit.txt"
        exit "$code"
    - name: Execute admit
      id: admit
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PYCOMMAND'
        mode='admit';target='engine/io/wp002-integration.ts'
        import os,pathlib,subprocess,sys,threading,json,hashlib
        p=pathlib.Path(os.environ['FS_EVIDENCE']);args=[os.environ['FS_NODE'],os.environ['FS_TSX'],target,mode]
        child=subprocess.Popen(args,cwd=pathlib.Path(os.environ['TASK_ROOT'],'mirror'),stdout=subprocess.PIPE,stderr=subprocess.PIPE)
        def drain(source,name,dest):
         with (p/(mode+'.'+name+'.txt')).open('wb') as f:
          for row in iter(lambda:source.read(65536),b''):
           f.write(row);f.flush()
           if os.environ.get('FS_EVIDENCE_MODE')!='bundle-v1':dest.write(row);dest.flush()
        workers=[threading.Thread(target=drain,args=(child.stdout,'stdout',sys.stdout.buffer)),threading.Thread(target=drain,args=(child.stderr,'stderr',sys.stderr.buffer))]
        for w in workers:w.start()
        status=child.wait()
        for w in workers:w.join()
        (p/(mode+'.exit.txt')).write_text(str(status)+'\n')
        receipt={'mode':mode,'args':args,'status':status}
        for stream in ['stdout','stderr']:
         b=(p/(mode+'.'+stream+'.txt')).read_bytes();receipt[stream]={'bytes':len(b),'sha256':hashlib.sha256(b).hexdigest()}
        (p/(mode+'.command.json')).write_text(json.dumps(receipt)+'\n')
        raise SystemExit(status)
        PYCOMMAND
    - name: Verify source mirror and live main unchanged
      id: preservation
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        python3 "$GITHUB_WORKSPACE/scripts/wp002-preflight.py" preserve
    - name: Remove temporary execution data
      id: cleanup
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PY'
        import os,pathlib,shutil,json
        p=pathlib.Path(os.environ['TASK_ROOT']);assert p.parent==pathlib.Path(os.environ['RUNNER_TEMP']) and p.name.startswith('fs24-')
        for x in p.iterdir():
         if x.name=='evidence':continue
         if x.is_dir():shutil.rmtree(x)
         else:x.unlink()
        (p/'evidence/cleanup.json').write_text(json.dumps({'executionDataRemoved':True,'reportDataRemainingUntilFrame':True})+'\n')
        PY
    - name: Emit complete evidence bytes
      id: evidence
      if: ${{ always() }}
      shell: bash
      env:
        FS_STEPS: ${{ toJSON(steps) }}
      run: |
        set -euo pipefail
        python3 - <<'PY'
        import os,pathlib,json,hashlib,base64,re
        p=pathlib.Path(os.environ['FS_EVIDENCE']);steps=json.loads(os.environ['FS_STEPS']);(p/'steps-at-emission.json').write_text(json.dumps(steps,indent=2)+'\n')
        # FS24C_REQUIRED_BEGIN
        required=['preflight', 'runtime', 'setup', 'install', 'admit', 'preservation', 'cleanup']
        ok=all(steps.get(s,{}).get('outcome')=='success' for s in required)
        # FS24C_REQUIRED_END
        errors=[]
        for f in sorted(p.glob('*.stderr.txt')):
         for line in f.read_text(errors='replace').splitlines():
          if line.strip():errors.append(line[:400])
        for f in sorted(p.glob('*.stdout.txt')):
         for line in f.read_text(errors='replace').splitlines():
          if re.search(r'error TS\d+|AssertionError|Error:',line):errors.append(line[:400])
        def redact(x):
         if re.search(r'\b(?:sk-[A-Za-z0-9_-]{16,}|gh[pousr]_[A-Za-z0-9]{20,}|AKIA[A-Z0-9]{16})\b',x) or ('PRIVATE'+' KEY') in x:return '[redacted]'
         if any(not re.fullmatch('[a-fA-F0-9]+',v) for v in re.findall(r'[A-Za-z0-9+/]{80,}={0,2}',x)):return '[redacted]'
         return x
        summary={'head':os.environ['FS_HEAD'],'base':os.environ.get('FS_BASE',''),'job':os.environ['FS_JOB'],'result':'success' if ok else 'failure','errors':[redact(x) for x in errors[:20]],'finalJobStatus':'requires-API-readback'}
        (p/'job-summary.json').write_text(json.dumps(summary,ensure_ascii=False)+'\n')
        with open(os.environ['GITHUB_OUTPUT'],'a') as out:out.write('summary='+json.dumps(summary,ensure_ascii=True)+'\n')
        identity=['FS24-D',os.environ['GITHUB_RUN_ID'],os.environ['GITHUB_RUN_ATTEMPT'],os.environ['FS_HEAD'],os.environ['FS_JOB'],os.environ['FS_EVENT'],os.environ.get('FS_BASE','')]
        if os.environ.get('FS_EVIDENCE_MODE')=='bundle-v1':
         import subprocess
         target=pathlib.Path(os.environ['TASK_ROOT'],'evidence-bundle.zip')
         subprocess.run(['python3',str(pathlib.Path(os.environ['GITHUB_WORKSPACE'],'engine/io/evidence-transfer.py')),'pack','--directory',str(p),'--file',str(target)],check=True)
         raise SystemExit(0 if ok else 1)
        print('FS24_BEGIN\t'+'\t'.join(identity))
        files=sorted(p.iterdir())
        for f in files:
         assert f.is_file() and re.fullmatch('[A-Za-z0-9_.-]+',f.name)
         data=f.read_bytes();name=base64.b64encode(f.name.encode()).decode();print('FS24_FILE\t'+name+'\t'+str(len(data))+'\t'+hashlib.sha256(data).hexdigest());value=base64.b64encode(data).decode()
         for i in range(0,len(value),2048):print('FS24_DATA\t'+str(i//2048+1)+'\t'+value[i:i+2048])
         print('FS24_END\t'+name)
        print('FS24_COMPLETE\t'+str(len(files)))
        raise SystemExit(0 if ok else 1)
        PY
    - name: Preserve original evidence bundle
      id: evidence_bundle
      if: ${{ always() && env.FS_EVIDENCE_MODE == 'bundle-v1' }}
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: fs24e-${{ github.run_id }}-${{ github.run_attempt }}-${{ env.FS_JOB }}
        path: ${{ env.TASK_ROOT }}/evidence-bundle.zip
        retention-days: 7
        compression-level: 0
        if-no-files-found: error
    - name: Remove final report data
      id: final_cleanup
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        case "$TASK_ROOT" in "$RUNNER_TEMP"/fs24-*) rm -rf -- "$TASK_ROOT" ;; *) exit 1 ;; esac
        test ! -e "$TASK_ROOT"
        printf 'FS24_FINAL_REPORT_CLEANUP=pass;job=%s\n' "$FS_JOB"
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event_name == 'workflow_run' && github.event.workflow_run.event == 'push' && github.event.workflow_run.head_branch
      == 'main' && github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.run_attempt == 1) }}
  producer-left:
    name: producer-left
    runs-on: ubuntu-24.04
    env:
      FS_JOB: producer-left
      FS_PRODUCER: left
    steps:
    - name: Initialize evidence directory
      id: init
      shell: bash
      run: |
        set -euo pipefail
        task_root="$(mktemp -d "$RUNNER_TEMP/fs24-$FS_JOB.XXXXXX")"
        mkdir "$task_root/evidence"
        printf 'TASK_ROOT=%s
        FS_EVIDENCE=%s/evidence
        ' "$task_root" "$task_root" >> "$GITHUB_ENV"
    - name: Checkout exact candidate
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        ref: ${{ env.FS_HEAD }}
        fetch-depth: 0
        persist-credentials: false
    - name: Read-only source admission
      id: preflight
      shell: bash
      run: |
        set -euo pipefail
        python3 "$GITHUB_WORKSPACE/scripts/wp002-preflight.py" runtime
    - name: Checksum-pinned runtime
      id: runtime
      shell: bash
      run: |
        set -euo pipefail
        curl --fail --silent --show-error --location https://nodejs.org/dist/v20.20.2/node-v20.20.2-linux-x64.tar.xz -o "$TASK_ROOT/node.tar.xz"
        printf '%s  %s\n' df770b2a6f130ed8627c9782c988fda9669fa23898329a61a871e32f965e007d "$TASK_ROOT/node.tar.xz" | sha256sum -c -
        tar -xJf "$TASK_ROOT/node.tar.xz" -C "$TASK_ROOT"
        node_path="$TASK_ROOT/node-v20.20.2-linux-x64/bin/node"
        npm_cli="$TASK_ROOT/node-v20.20.2-linux-x64/lib/node_modules/npm/bin/npm-cli.js"
        test "$($node_path --version)" = v20.20.2
        test "$($node_path "$npm_cli" --version)" = 10.8.2
        printf 'FS_NODE=%s\nFS_NPM=%s\n' "$node_path" "$npm_cli" >> "$GITHUB_ENV"
        python3 - <<'PY'
        import os,json,pathlib
        pathlib.Path(os.environ['FS_EVIDENCE'],'runtime.json').write_text(json.dumps({'node':'20.20.2','npm':'10.8.2','archiveSha256':'df770b2a6f130ed8627c9782c988fda9669fa23898329a61a871e32f965e007d','archiveChecksum':'pass','node20EolException':'FS24-D-only','actionNode24Patch':'unproven','runnerOS':os.environ.get('RUNNER_OS'),'runnerArch':os.environ.get('RUNNER_ARCH'),'imageVersion':os.environ.get('ImageVersion'),'billedCostUsd':None,'B1B2':'unverified'})+'\n')
        PY
    - name: Set up action runtime tooling
      id: setup
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
      with:
        node-version: '20'
    - name: Install exact lockfile without lifecycle scripts
      id: install
      shell: bash
      run: |
        set -euo pipefail
        mkdir "$TASK_ROOT/mirror"
        git archive "$FS_HEAD" | tar -x -C "$TASK_ROOT/mirror"
        printf 'FS_TSX=%s/mirror/node_modules/tsx/dist/cli.mjs\n' "$TASK_ROOT" >> "$GITHUB_ENV"
        export npm_config_cache="$TASK_ROOT/npm-cache"
        set +e
        "$FS_NODE" "$FS_NPM" ci --prefix "$TASK_ROOT/mirror" --ignore-scripts --no-audit --no-fund --include=dev --include=optional > "$FS_EVIDENCE/npm-ci.stdout.txt" 2> "$FS_EVIDENCE/npm-ci.stderr.txt"
        code=$?
        set -e
        printf '%s\n' "$code" > "$FS_EVIDENCE/npm-ci.exit.txt"
        exit "$code"
    - name: Execute prepare
      id: prepare
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PYCOMMAND'
        mode='prepare';target='engine/io/wp002-integration.ts'
        import os,pathlib,subprocess,sys,threading,json,hashlib
        p=pathlib.Path(os.environ['FS_EVIDENCE']);args=[os.environ['FS_NODE'],os.environ['FS_TSX'],target,mode]
        child=subprocess.Popen(args,cwd=pathlib.Path(os.environ['TASK_ROOT'],'mirror'),stdout=subprocess.PIPE,stderr=subprocess.PIPE)
        def drain(source,name,dest):
         with (p/(mode+'.'+name+'.txt')).open('wb') as f:
          for row in iter(lambda:source.read(65536),b''):
           f.write(row);f.flush()
           if os.environ.get('FS_EVIDENCE_MODE')!='bundle-v1':dest.write(row);dest.flush()
        workers=[threading.Thread(target=drain,args=(child.stdout,'stdout',sys.stdout.buffer)),threading.Thread(target=drain,args=(child.stderr,'stderr',sys.stderr.buffer))]
        for w in workers:w.start()
        status=child.wait()
        for w in workers:w.join()
        (p/(mode+'.exit.txt')).write_text(str(status)+'\n')
        receipt={'mode':mode,'args':args,'status':status}
        for stream in ['stdout','stderr']:
         b=(p/(mode+'.'+stream+'.txt')).read_bytes();receipt[stream]={'bytes':len(b),'sha256':hashlib.sha256(b).hexdigest()}
        (p/(mode+'.command.json')).write_text(json.dumps(receipt)+'\n')
        raise SystemExit(status)
        PYCOMMAND
      if: ${{ env.FS_CLOSURE != 'true' && env.FS_EVIDENCE_MODE != 'bundle-v1' }}
    - name: Upload new bounded fixture
      id: upload
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: ${{ steps.prepare.outputs.artifact_name }}
        path: ${{ env.TASK_ROOT }}/handoff/*
        retention-days: 7
        if-no-files-found: error
        compression-level: 0
      if: ${{ env.FS_CLOSURE != 'true' && env.FS_EVIDENCE_MODE != 'bundle-v1' }}
    - name: Execute receive
      id: receive
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PYCOMMAND'
        mode='receive';target='engine/io/wp002-integration.ts'
        import os,pathlib,subprocess,sys,threading,json,hashlib
        p=pathlib.Path(os.environ['FS_EVIDENCE']);args=[os.environ['FS_NODE'],os.environ['FS_TSX'],target,mode]
        child=subprocess.Popen(args,cwd=pathlib.Path(os.environ['TASK_ROOT'],'mirror'),stdout=subprocess.PIPE,stderr=subprocess.PIPE)
        def drain(source,name,dest):
         with (p/(mode+'.'+name+'.txt')).open('wb') as f:
          for row in iter(lambda:source.read(65536),b''):
           f.write(row);f.flush()
           if os.environ.get('FS_EVIDENCE_MODE')!='bundle-v1':dest.write(row);dest.flush()
        workers=[threading.Thread(target=drain,args=(child.stdout,'stdout',sys.stdout.buffer)),threading.Thread(target=drain,args=(child.stderr,'stderr',sys.stderr.buffer))]
        for w in workers:w.start()
        status=child.wait()
        for w in workers:w.join()
        (p/(mode+'.exit.txt')).write_text(str(status)+'\n')
        receipt={'mode':mode,'args':args,'status':status}
        for stream in ['stdout','stderr']:
         b=(p/(mode+'.'+stream+'.txt')).read_bytes();receipt[stream]={'bytes':len(b),'sha256':hashlib.sha256(b).hexdigest()}
        (p/(mode+'.command.json')).write_text(json.dumps(receipt)+'\n')
        raise SystemExit(status)
        PYCOMMAND
      env:
        FS_ARTIFACT_ID: ${{ steps.upload.outputs.artifact-id }}
      if: ${{ env.FS_CLOSURE != 'true' && env.FS_EVIDENCE_MODE != 'bundle-v1' }}
    - name: Verify source mirror and live main unchanged
      id: preservation
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        python3 "$GITHUB_WORKSPACE/scripts/wp002-preflight.py" preserve
    - name: Remove temporary execution data
      id: cleanup
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PY'
        import os,pathlib,shutil,json
        p=pathlib.Path(os.environ['TASK_ROOT']);assert p.parent==pathlib.Path(os.environ['RUNNER_TEMP']) and p.name.startswith('fs24-')
        for x in p.iterdir():
         if x.name=='evidence':continue
         if x.is_dir():shutil.rmtree(x)
         else:x.unlink()
        (p/'evidence/cleanup.json').write_text(json.dumps({'executionDataRemoved':True,'reportDataRemainingUntilFrame':True})+'\n')
        PY
    - name: Emit complete evidence bytes
      id: evidence
      if: ${{ always() }}
      shell: bash
      env:
        FS_STEPS: ${{ toJSON(steps) }}
      run: |
        set -euo pipefail
        python3 - <<'PY'
        import os,pathlib,json,hashlib,base64,re
        p=pathlib.Path(os.environ['FS_EVIDENCE']);steps=json.loads(os.environ['FS_STEPS']);(p/'steps-at-emission.json').write_text(json.dumps(steps,indent=2)+'\n')
        # FS24C_REQUIRED_BEGIN
        required=['preflight', 'runtime', 'setup', 'install', 'prepare', 'upload', 'receive', 'preservation', 'cleanup']
        ok=all(steps.get(s,{}).get('outcome')=='success' for s in required)
        # FS24C_REQUIRED_END
        errors=[]
        for f in sorted(p.glob('*.stderr.txt')):
         for line in f.read_text(errors='replace').splitlines():
          if line.strip():errors.append(line[:400])
        for f in sorted(p.glob('*.stdout.txt')):
         for line in f.read_text(errors='replace').splitlines():
          if re.search(r'error TS\d+|AssertionError|Error:',line):errors.append(line[:400])
        def redact(x):
         if re.search(r'\b(?:sk-[A-Za-z0-9_-]{16,}|gh[pousr]_[A-Za-z0-9]{20,}|AKIA[A-Z0-9]{16})\b',x) or ('PRIVATE'+' KEY') in x:return '[redacted]'
         if any(not re.fullmatch('[a-fA-F0-9]+',v) for v in re.findall(r'[A-Za-z0-9+/]{80,}={0,2}',x)):return '[redacted]'
         return x
        summary={'head':os.environ['FS_HEAD'],'base':os.environ.get('FS_BASE',''),'job':os.environ['FS_JOB'],'result':'success' if ok else 'failure','errors':[redact(x) for x in errors[:20]],'finalJobStatus':'requires-API-readback'}
        (p/'job-summary.json').write_text(json.dumps(summary,ensure_ascii=False)+'\n')
        with open(os.environ['GITHUB_OUTPUT'],'a') as out:out.write('summary='+json.dumps(summary,ensure_ascii=True)+'\n')
        identity=['FS24-D',os.environ['GITHUB_RUN_ID'],os.environ['GITHUB_RUN_ATTEMPT'],os.environ['FS_HEAD'],os.environ['FS_JOB'],os.environ['FS_EVENT'],os.environ.get('FS_BASE','')]
        if os.environ.get('FS_EVIDENCE_MODE')=='bundle-v1':
         import subprocess
         target=pathlib.Path(os.environ['TASK_ROOT'],'evidence-bundle.zip')
         subprocess.run(['python3',str(pathlib.Path(os.environ['GITHUB_WORKSPACE'],'engine/io/evidence-transfer.py')),'pack','--directory',str(p),'--file',str(target)],check=True)
         raise SystemExit(0 if ok else 1)
        print('FS24_BEGIN\t'+'\t'.join(identity))
        files=sorted(p.iterdir())
        for f in files:
         assert f.is_file() and re.fullmatch('[A-Za-z0-9_.-]+',f.name)
         data=f.read_bytes();name=base64.b64encode(f.name.encode()).decode();print('FS24_FILE\t'+name+'\t'+str(len(data))+'\t'+hashlib.sha256(data).hexdigest());value=base64.b64encode(data).decode()
         for i in range(0,len(value),2048):print('FS24_DATA\t'+str(i//2048+1)+'\t'+value[i:i+2048])
         print('FS24_END\t'+name)
        print('FS24_COMPLETE\t'+str(len(files)))
        raise SystemExit(0 if ok else 1)
        PY
    - name: Preserve original evidence bundle
      id: evidence_bundle
      if: ${{ always() && env.FS_EVIDENCE_MODE == 'bundle-v1' }}
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: fs24e-${{ github.run_id }}-${{ github.run_attempt }}-${{ env.FS_JOB }}
        path: ${{ env.TASK_ROOT }}/evidence-bundle.zip
        retention-days: 7
        compression-level: 0
        if-no-files-found: error
    - name: Remove final report data
      id: final_cleanup
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        case "$TASK_ROOT" in "$RUNNER_TEMP"/fs24-*) rm -rf -- "$TASK_ROOT" ;; *) exit 1 ;; esac
        test ! -e "$TASK_ROOT"
        printf 'FS24_FINAL_REPORT_CLEANUP=pass;job=%s\n' "$FS_JOB"
    needs:
    - admit
    if: ${{ github.event_name == 'workflow_dispatch' && needs.admit.result == 'success' }}
    outputs:
      artifact: ${{ steps.upload.outputs.artifact-id }}
      hash: ${{ steps.prepare.outputs.manifest_hash }}
  producer-right:
    name: producer-right
    runs-on: ubuntu-24.04
    env:
      FS_JOB: producer-right
      FS_PRODUCER: right
    steps:
    - name: Initialize evidence directory
      id: init
      shell: bash
      run: |
        set -euo pipefail
        task_root="$(mktemp -d "$RUNNER_TEMP/fs24-$FS_JOB.XXXXXX")"
        mkdir "$task_root/evidence"
        printf 'TASK_ROOT=%s
        FS_EVIDENCE=%s/evidence
        ' "$task_root" "$task_root" >> "$GITHUB_ENV"
    - name: Checkout exact candidate
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        ref: ${{ env.FS_HEAD }}
        fetch-depth: 0
        persist-credentials: false
    - name: Read-only source admission
      id: preflight
      shell: bash
      run: |
        set -euo pipefail
        python3 "$GITHUB_WORKSPACE/scripts/wp002-preflight.py" runtime
    - name: Checksum-pinned runtime
      id: runtime
      shell: bash
      run: |
        set -euo pipefail
        curl --fail --silent --show-error --location https://nodejs.org/dist/v20.20.2/node-v20.20.2-linux-x64.tar.xz -o "$TASK_ROOT/node.tar.xz"
        printf '%s  %s\n' df770b2a6f130ed8627c9782c988fda9669fa23898329a61a871e32f965e007d "$TASK_ROOT/node.tar.xz" | sha256sum -c -
        tar -xJf "$TASK_ROOT/node.tar.xz" -C "$TASK_ROOT"
        node_path="$TASK_ROOT/node-v20.20.2-linux-x64/bin/node"
        npm_cli="$TASK_ROOT/node-v20.20.2-linux-x64/lib/node_modules/npm/bin/npm-cli.js"
        test "$($node_path --version)" = v20.20.2
        test "$($node_path "$npm_cli" --version)" = 10.8.2
        printf 'FS_NODE=%s\nFS_NPM=%s\n' "$node_path" "$npm_cli" >> "$GITHUB_ENV"
        python3 - <<'PY'
        import os,json,pathlib
        pathlib.Path(os.environ['FS_EVIDENCE'],'runtime.json').write_text(json.dumps({'node':'20.20.2','npm':'10.8.2','archiveSha256':'df770b2a6f130ed8627c9782c988fda9669fa23898329a61a871e32f965e007d','archiveChecksum':'pass','node20EolException':'FS24-D-only','actionNode24Patch':'unproven','runnerOS':os.environ.get('RUNNER_OS'),'runnerArch':os.environ.get('RUNNER_ARCH'),'imageVersion':os.environ.get('ImageVersion'),'billedCostUsd':None,'B1B2':'unverified'})+'\n')
        PY
    - name: Set up action runtime tooling
      id: setup
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
      with:
        node-version: '20'
    - name: Install exact lockfile without lifecycle scripts
      id: install
      shell: bash
      run: |
        set -euo pipefail
        mkdir "$TASK_ROOT/mirror"
        git archive "$FS_HEAD" | tar -x -C "$TASK_ROOT/mirror"
        printf 'FS_TSX=%s/mirror/node_modules/tsx/dist/cli.mjs\n' "$TASK_ROOT" >> "$GITHUB_ENV"
        export npm_config_cache="$TASK_ROOT/npm-cache"
        set +e
        "$FS_NODE" "$FS_NPM" ci --prefix "$TASK_ROOT/mirror" --ignore-scripts --no-audit --no-fund --include=dev --include=optional > "$FS_EVIDENCE/npm-ci.stdout.txt" 2> "$FS_EVIDENCE/npm-ci.stderr.txt"
        code=$?
        set -e
        printf '%s\n' "$code" > "$FS_EVIDENCE/npm-ci.exit.txt"
        exit "$code"
    - name: Execute prepare
      id: prepare
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PYCOMMAND'
        mode='prepare';target='engine/io/wp002-integration.ts'
        import os,pathlib,subprocess,sys,threading,json,hashlib
        p=pathlib.Path(os.environ['FS_EVIDENCE']);args=[os.environ['FS_NODE'],os.environ['FS_TSX'],target,mode]
        child=subprocess.Popen(args,cwd=pathlib.Path(os.environ['TASK_ROOT'],'mirror'),stdout=subprocess.PIPE,stderr=subprocess.PIPE)
        def drain(source,name,dest):
         with (p/(mode+'.'+name+'.txt')).open('wb') as f:
          for row in iter(lambda:source.read(65536),b''):
           f.write(row);f.flush()
           if os.environ.get('FS_EVIDENCE_MODE')!='bundle-v1':dest.write(row);dest.flush()
        workers=[threading.Thread(target=drain,args=(child.stdout,'stdout',sys.stdout.buffer)),threading.Thread(target=drain,args=(child.stderr,'stderr',sys.stderr.buffer))]
        for w in workers:w.start()
        status=child.wait()
        for w in workers:w.join()
        (p/(mode+'.exit.txt')).write_text(str(status)+'\n')
        receipt={'mode':mode,'args':args,'status':status}
        for stream in ['stdout','stderr']:
         b=(p/(mode+'.'+stream+'.txt')).read_bytes();receipt[stream]={'bytes':len(b),'sha256':hashlib.sha256(b).hexdigest()}
        (p/(mode+'.command.json')).write_text(json.dumps(receipt)+'\n')
        raise SystemExit(status)
        PYCOMMAND
      if: ${{ env.FS_CLOSURE != 'true' && env.FS_EVIDENCE_MODE != 'bundle-v1' }}
    - name: Upload new bounded fixture
      id: upload
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: ${{ steps.prepare.outputs.artifact_name }}
        path: ${{ env.TASK_ROOT }}/handoff/*
        retention-days: 7
        if-no-files-found: error
        compression-level: 0
      if: ${{ env.FS_CLOSURE != 'true' && env.FS_EVIDENCE_MODE != 'bundle-v1' }}
    - name: Execute receive
      id: receive
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PYCOMMAND'
        mode='receive';target='engine/io/wp002-integration.ts'
        import os,pathlib,subprocess,sys,threading,json,hashlib
        p=pathlib.Path(os.environ['FS_EVIDENCE']);args=[os.environ['FS_NODE'],os.environ['FS_TSX'],target,mode]
        child=subprocess.Popen(args,cwd=pathlib.Path(os.environ['TASK_ROOT'],'mirror'),stdout=subprocess.PIPE,stderr=subprocess.PIPE)
        def drain(source,name,dest):
         with (p/(mode+'.'+name+'.txt')).open('wb') as f:
          for row in iter(lambda:source.read(65536),b''):
           f.write(row);f.flush()
           if os.environ.get('FS_EVIDENCE_MODE')!='bundle-v1':dest.write(row);dest.flush()
        workers=[threading.Thread(target=drain,args=(child.stdout,'stdout',sys.stdout.buffer)),threading.Thread(target=drain,args=(child.stderr,'stderr',sys.stderr.buffer))]
        for w in workers:w.start()
        status=child.wait()
        for w in workers:w.join()
        (p/(mode+'.exit.txt')).write_text(str(status)+'\n')
        receipt={'mode':mode,'args':args,'status':status}
        for stream in ['stdout','stderr']:
         b=(p/(mode+'.'+stream+'.txt')).read_bytes();receipt[stream]={'bytes':len(b),'sha256':hashlib.sha256(b).hexdigest()}
        (p/(mode+'.command.json')).write_text(json.dumps(receipt)+'\n')
        raise SystemExit(status)
        PYCOMMAND
      env:
        FS_ARTIFACT_ID: ${{ steps.upload.outputs.artifact-id }}
      if: ${{ env.FS_CLOSURE != 'true' && env.FS_EVIDENCE_MODE != 'bundle-v1' }}
    - name: Verify source mirror and live main unchanged
      id: preservation
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        python3 "$GITHUB_WORKSPACE/scripts/wp002-preflight.py" preserve
    - name: Remove temporary execution data
      id: cleanup
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PY'
        import os,pathlib,shutil,json
        p=pathlib.Path(os.environ['TASK_ROOT']);assert p.parent==pathlib.Path(os.environ['RUNNER_TEMP']) and p.name.startswith('fs24-')
        for x in p.iterdir():
         if x.name=='evidence':continue
         if x.is_dir():shutil.rmtree(x)
         else:x.unlink()
        (p/'evidence/cleanup.json').write_text(json.dumps({'executionDataRemoved':True,'reportDataRemainingUntilFrame':True})+'\n')
        PY
    - name: Emit complete evidence bytes
      id: evidence
      if: ${{ always() }}
      shell: bash
      env:
        FS_STEPS: ${{ toJSON(steps) }}
      run: |
        set -euo pipefail
        python3 - <<'PY'
        import os,pathlib,json,hashlib,base64,re
        p=pathlib.Path(os.environ['FS_EVIDENCE']);steps=json.loads(os.environ['FS_STEPS']);(p/'steps-at-emission.json').write_text(json.dumps(steps,indent=2)+'\n')
        # FS24C_REQUIRED_BEGIN
        required=['preflight', 'runtime', 'setup', 'install', 'prepare', 'upload', 'receive', 'preservation', 'cleanup']
        ok=all(steps.get(s,{}).get('outcome')=='success' for s in required)
        # FS24C_REQUIRED_END
        errors=[]
        for f in sorted(p.glob('*.stderr.txt')):
         for line in f.read_text(errors='replace').splitlines():
          if line.strip():errors.append(line[:400])
        for f in sorted(p.glob('*.stdout.txt')):
         for line in f.read_text(errors='replace').splitlines():
          if re.search(r'error TS\d+|AssertionError|Error:',line):errors.append(line[:400])
        def redact(x):
         if re.search(r'\b(?:sk-[A-Za-z0-9_-]{16,}|gh[pousr]_[A-Za-z0-9]{20,}|AKIA[A-Z0-9]{16})\b',x) or ('PRIVATE'+' KEY') in x:return '[redacted]'
         if any(not re.fullmatch('[a-fA-F0-9]+',v) for v in re.findall(r'[A-Za-z0-9+/]{80,}={0,2}',x)):return '[redacted]'
         return x
        summary={'head':os.environ['FS_HEAD'],'base':os.environ.get('FS_BASE',''),'job':os.environ['FS_JOB'],'result':'success' if ok else 'failure','errors':[redact(x) for x in errors[:20]],'finalJobStatus':'requires-API-readback'}
        (p/'job-summary.json').write_text(json.dumps(summary,ensure_ascii=False)+'\n')
        with open(os.environ['GITHUB_OUTPUT'],'a') as out:out.write('summary='+json.dumps(summary,ensure_ascii=True)+'\n')
        identity=['FS24-D',os.environ['GITHUB_RUN_ID'],os.environ['GITHUB_RUN_ATTEMPT'],os.environ['FS_HEAD'],os.environ['FS_JOB'],os.environ['FS_EVENT'],os.environ.get('FS_BASE','')]
        if os.environ.get('FS_EVIDENCE_MODE')=='bundle-v1':
         import subprocess
         target=pathlib.Path(os.environ['TASK_ROOT'],'evidence-bundle.zip')
         subprocess.run(['python3',str(pathlib.Path(os.environ['GITHUB_WORKSPACE'],'engine/io/evidence-transfer.py')),'pack','--directory',str(p),'--file',str(target)],check=True)
         raise SystemExit(0 if ok else 1)
        print('FS24_BEGIN\t'+'\t'.join(identity))
        files=sorted(p.iterdir())
        for f in files:
         assert f.is_file() and re.fullmatch('[A-Za-z0-9_.-]+',f.name)
         data=f.read_bytes();name=base64.b64encode(f.name.encode()).decode();print('FS24_FILE\t'+name+'\t'+str(len(data))+'\t'+hashlib.sha256(data).hexdigest());value=base64.b64encode(data).decode()
         for i in range(0,len(value),2048):print('FS24_DATA\t'+str(i//2048+1)+'\t'+value[i:i+2048])
         print('FS24_END\t'+name)
        print('FS24_COMPLETE\t'+str(len(files)))
        raise SystemExit(0 if ok else 1)
        PY
    - name: Preserve original evidence bundle
      id: evidence_bundle
      if: ${{ always() && env.FS_EVIDENCE_MODE == 'bundle-v1' }}
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: fs24e-${{ github.run_id }}-${{ github.run_attempt }}-${{ env.FS_JOB }}
        path: ${{ env.TASK_ROOT }}/evidence-bundle.zip
        retention-days: 7
        compression-level: 0
        if-no-files-found: error
    - name: Remove final report data
      id: final_cleanup
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        case "$TASK_ROOT" in "$RUNNER_TEMP"/fs24-*) rm -rf -- "$TASK_ROOT" ;; *) exit 1 ;; esac
        test ! -e "$TASK_ROOT"
        printf 'FS24_FINAL_REPORT_CLEANUP=pass;job=%s\n' "$FS_JOB"
    needs:
    - admit
    if: ${{ github.event_name == 'workflow_dispatch' && needs.admit.result == 'success' }}
    outputs:
      artifact: ${{ steps.upload.outputs.artifact-id }}
      hash: ${{ steps.prepare.outputs.manifest_hash }}
  controller:
    name: controller
    runs-on: ubuntu-24.04
    env:
      FS_JOB: controller
      FS_LEFT_ARTIFACT: ${{ needs.producer-left.outputs.artifact }}
      FS_RIGHT_ARTIFACT: ${{ needs.producer-right.outputs.artifact }}
      FS_LEFT_HASH: ${{ needs.producer-left.outputs.hash }}
      FS_RIGHT_HASH: ${{ needs.producer-right.outputs.hash }}
    steps:
    - name: Initialize evidence directory
      id: init
      shell: bash
      run: |
        set -euo pipefail
        task_root="$(mktemp -d "$RUNNER_TEMP/fs24-$FS_JOB.XXXXXX")"
        mkdir "$task_root/evidence"
        printf 'TASK_ROOT=%s
        FS_EVIDENCE=%s/evidence
        ' "$task_root" "$task_root" >> "$GITHUB_ENV"
    - name: Checkout exact candidate
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        ref: ${{ env.FS_HEAD }}
        fetch-depth: 0
        persist-credentials: false
    - name: Read-only source admission
      id: preflight
      shell: bash
      run: |
        set -euo pipefail
        python3 "$GITHUB_WORKSPACE/scripts/wp002-preflight.py" runtime
    - name: Checksum-pinned runtime
      id: runtime
      shell: bash
      run: |
        set -euo pipefail
        curl --fail --silent --show-error --location https://nodejs.org/dist/v20.20.2/node-v20.20.2-linux-x64.tar.xz -o "$TASK_ROOT/node.tar.xz"
        printf '%s  %s\n' df770b2a6f130ed8627c9782c988fda9669fa23898329a61a871e32f965e007d "$TASK_ROOT/node.tar.xz" | sha256sum -c -
        tar -xJf "$TASK_ROOT/node.tar.xz" -C "$TASK_ROOT"
        node_path="$TASK_ROOT/node-v20.20.2-linux-x64/bin/node"
        npm_cli="$TASK_ROOT/node-v20.20.2-linux-x64/lib/node_modules/npm/bin/npm-cli.js"
        test "$($node_path --version)" = v20.20.2
        test "$($node_path "$npm_cli" --version)" = 10.8.2
        printf 'FS_NODE=%s\nFS_NPM=%s\n' "$node_path" "$npm_cli" >> "$GITHUB_ENV"
        python3 - <<'PY'
        import os,json,pathlib
        pathlib.Path(os.environ['FS_EVIDENCE'],'runtime.json').write_text(json.dumps({'node':'20.20.2','npm':'10.8.2','archiveSha256':'df770b2a6f130ed8627c9782c988fda9669fa23898329a61a871e32f965e007d','archiveChecksum':'pass','node20EolException':'FS24-D-only','actionNode24Patch':'unproven','runnerOS':os.environ.get('RUNNER_OS'),'runnerArch':os.environ.get('RUNNER_ARCH'),'imageVersion':os.environ.get('ImageVersion'),'billedCostUsd':None,'B1B2':'unverified'})+'\n')
        PY
    - name: Set up action runtime tooling
      id: setup
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
      with:
        node-version: '20'
    - name: Install exact lockfile without lifecycle scripts
      id: install
      shell: bash
      run: |
        set -euo pipefail
        mkdir "$TASK_ROOT/mirror"
        git archive "$FS_HEAD" | tar -x -C "$TASK_ROOT/mirror"
        printf 'FS_TSX=%s/mirror/node_modules/tsx/dist/cli.mjs\n' "$TASK_ROOT" >> "$GITHUB_ENV"
        export npm_config_cache="$TASK_ROOT/npm-cache"
        set +e
        "$FS_NODE" "$FS_NPM" ci --prefix "$TASK_ROOT/mirror" --ignore-scripts --no-audit --no-fund --include=dev --include=optional > "$FS_EVIDENCE/npm-ci.stdout.txt" 2> "$FS_EVIDENCE/npm-ci.stderr.txt"
        code=$?
        set -e
        printf '%s\n' "$code" > "$FS_EVIDENCE/npm-ci.exit.txt"
        exit "$code"
    - name: Execute controller
      id: controller
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PYCOMMAND'
        mode='controller';target='engine/io/wp002-integration.ts'
        import os,pathlib,subprocess,sys,threading,json,hashlib
        p=pathlib.Path(os.environ['FS_EVIDENCE']);args=[os.environ['FS_NODE'],os.environ['FS_TSX'],target,mode]
        child=subprocess.Popen(args,cwd=pathlib.Path(os.environ['TASK_ROOT'],'mirror'),stdout=subprocess.PIPE,stderr=subprocess.PIPE)
        def drain(source,name,dest):
         with (p/(mode+'.'+name+'.txt')).open('wb') as f:
          for row in iter(lambda:source.read(65536),b''):
           f.write(row);f.flush()
           if os.environ.get('FS_EVIDENCE_MODE')!='bundle-v1':dest.write(row);dest.flush()
        workers=[threading.Thread(target=drain,args=(child.stdout,'stdout',sys.stdout.buffer)),threading.Thread(target=drain,args=(child.stderr,'stderr',sys.stderr.buffer))]
        for w in workers:w.start()
        status=child.wait()
        for w in workers:w.join()
        (p/(mode+'.exit.txt')).write_text(str(status)+'\n')
        receipt={'mode':mode,'args':args,'status':status}
        for stream in ['stdout','stderr']:
         b=(p/(mode+'.'+stream+'.txt')).read_bytes();receipt[stream]={'bytes':len(b),'sha256':hashlib.sha256(b).hexdigest()}
        (p/(mode+'.command.json')).write_text(json.dumps(receipt)+'\n')
        raise SystemExit(status)
        PYCOMMAND
    - name: Verify source mirror and live main unchanged
      id: preservation
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        python3 "$GITHUB_WORKSPACE/scripts/wp002-preflight.py" preserve
    - name: Remove temporary execution data
      id: cleanup
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        python3 - <<'PY'
        import os,pathlib,shutil,json
        p=pathlib.Path(os.environ['TASK_ROOT']);assert p.parent==pathlib.Path(os.environ['RUNNER_TEMP']) and p.name.startswith('fs24-')
        for x in p.iterdir():
         if x.name=='evidence':continue
         if x.is_dir():shutil.rmtree(x)
         else:x.unlink()
        (p/'evidence/cleanup.json').write_text(json.dumps({'executionDataRemoved':True,'reportDataRemainingUntilFrame':True})+'\n')
        PY
    - name: Emit complete evidence bytes
      id: evidence
      if: ${{ always() }}
      shell: bash
      env:
        FS_STEPS: ${{ toJSON(steps) }}
      run: |
        set -euo pipefail
        python3 - <<'PY'
        import os,pathlib,json,hashlib,base64,re
        p=pathlib.Path(os.environ['FS_EVIDENCE']);steps=json.loads(os.environ['FS_STEPS']);(p/'steps-at-emission.json').write_text(json.dumps(steps,indent=2)+'\n')
        # FS24C_REQUIRED_BEGIN
        required=['preflight', 'runtime', 'setup', 'install', 'controller', 'preservation', 'cleanup']
        ok=all(steps.get(s,{}).get('outcome')=='success' for s in required)
        # FS24C_REQUIRED_END
        errors=[]
        for f in sorted(p.glob('*.stderr.txt')):
         for line in f.read_text(errors='replace').splitlines():
          if line.strip():errors.append(line[:400])
        for f in sorted(p.glob('*.stdout.txt')):
         for line in f.read_text(errors='replace').splitlines():
          if re.search(r'error TS\d+|AssertionError|Error:',line):errors.append(line[:400])
        def redact(x):
         if re.search(r'\b(?:sk-[A-Za-z0-9_-]{16,}|gh[pousr]_[A-Za-z0-9]{20,}|AKIA[A-Z0-9]{16})\b',x) or ('PRIVATE'+' KEY') in x:return '[redacted]'
         if any(not re.fullmatch('[a-fA-F0-9]+',v) for v in re.findall(r'[A-Za-z0-9+/]{80,}={0,2}',x)):return '[redacted]'
         return x
        summary={'head':os.environ['FS_HEAD'],'base':os.environ.get('FS_BASE',''),'job':os.environ['FS_JOB'],'result':'success' if ok else 'failure','errors':[redact(x) for x in errors[:20]],'finalJobStatus':'requires-API-readback'}
        (p/'job-summary.json').write_text(json.dumps(summary,ensure_ascii=False)+'\n')
        with open(os.environ['GITHUB_OUTPUT'],'a') as out:out.write('summary='+json.dumps(summary,ensure_ascii=True)+'\n')
        identity=['FS24-D',os.environ['GITHUB_RUN_ID'],os.environ['GITHUB_RUN_ATTEMPT'],os.environ['FS_HEAD'],os.environ['FS_JOB'],os.environ['FS_EVENT'],os.environ.get('FS_BASE','')]
        if os.environ.get('FS_EVIDENCE_MODE')=='bundle-v1':
         import subprocess
         target=pathlib.Path(os.environ['TASK_ROOT'],'evidence-bundle.zip')
         subprocess.run(['python3',str(pathlib.Path(os.environ['GITHUB_WORKSPACE'],'engine/io/evidence-transfer.py')),'pack','--directory',str(p),'--file',str(target)],check=True)
         raise SystemExit(0 if ok else 1)
        print('FS24_BEGIN\t'+'\t'.join(identity))
        files=sorted(p.iterdir())
        for f in files:
         assert f.is_file() and re.fullmatch('[A-Za-z0-9_.-]+',f.name)
         data=f.read_bytes();name=base64.b64encode(f.name.encode()).decode();print('FS24_FILE\t'+name+'\t'+str(len(data))+'\t'+hashlib.sha256(data).hexdigest());value=base64.b64encode(data).decode()
         for i in range(0,len(value),2048):print('FS24_DATA\t'+str(i//2048+1)+'\t'+value[i:i+2048])
         print('FS24_END\t'+name)
        print('FS24_COMPLETE\t'+str(len(files)))
        raise SystemExit(0 if ok else 1)
        PY
    - name: Preserve original evidence bundle
      id: evidence_bundle
      if: ${{ always() && env.FS_EVIDENCE_MODE == 'bundle-v1' }}
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: fs24e-${{ github.run_id }}-${{ github.run_attempt }}-${{ env.FS_JOB }}
        path: ${{ env.TASK_ROOT }}/evidence-bundle.zip
        retention-days: 7
        compression-level: 0
        if-no-files-found: error
    - name: Remove final report data
      id: final_cleanup
      if: ${{ always() }}
      shell: bash
      run: |
        set -euo pipefail
        case "$TASK_ROOT" in "$RUNNER_TEMP"/fs24-*) rm -rf -- "$TASK_ROOT" ;; *) exit 1 ;; esac
        test ! -e "$TASK_ROOT"
        printf 'FS24_FINAL_REPORT_CLEANUP=pass;job=%s\n' "$FS_JOB"
    needs:
    - producer-left
    - producer-right
    permissions:
      contents: read
      actions: write
      pull-requests: read
    if: ${{ github.event_name == 'workflow_dispatch' && needs.producer-left.result == 'success' && needs.producer-right.result == 'success' }}
  report:
    name: report
    needs:
    - foundation
    - admit
    - producer-left
    - producer-right
    - controller
    if: ${{ always() && (github.event_name == 'push' || github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch' || needs.admit.result
      != 'skipped') }}
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
    - name: Required outcomes
      shell: bash
      env:
        NEEDS: ${{ toJSON(needs) }}
      run: |
        python3 - <<'PYREPORT'
        import os,json
        needs=json.loads(os.environ['NEEDS']);required=['foundation'] if os.environ['GITHUB_EVENT_NAME'] in ['push','pull_request'] else (['admit'] if os.environ['GITHUB_EVENT_NAME']=='workflow_run' else ['admit','producer-left','producer-right','controller'])
        print(json.dumps({'grant':'FS24-D','needs':needs,'required':required,'WP002':'todo','ownerAcceptance':'pending'}))
        assert all(needs.get(x,{}).get('result')=='success' for x in required)
        PYREPORT
'on':
  push:
    branches:
    - wp/002
  pull_request:
    branches:
    - main
    types:
    - opened
    - synchronize
  workflow_run:
    workflows:
    - Fulcrum CI
    types:
    - completed
    branches:
    - main
  workflow_dispatch:
    inputs:
      request:
        required: true
        type: string
        description: FS24-D activation or resume input with request digest
run-name: ${{ inputs.request && format('FS24-D {0}', fromJSON(inputs.request).requestId) || github.event.pull_request.title || github.event.head_commit.message
  || 'FS24-D read-only post-merge' }}
